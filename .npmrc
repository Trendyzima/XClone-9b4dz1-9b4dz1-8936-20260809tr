# Project npm configuration
#
# Keep the dependency install portable across GitHub Actions, Vercel,
# local development, and other build runners. The Vercel build wrapper
# (_build.cjs) installs _preload.cjs explicitly with an absolute path when
# the preload hook is actually required.
legacy-peer-deps=true
