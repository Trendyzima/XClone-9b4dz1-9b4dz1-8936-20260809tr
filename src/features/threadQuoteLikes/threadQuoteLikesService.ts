import { supabase } from '@/lib/supabase';
export async function listThreadQuoteLikes(quoteId:string,limit=50){const {data,error}=await supabase.from('thread_quote_likes').select('id,user_id,quote_id,created_at').eq('quote_id',quoteId).order('created_at',{ascending:false}).limit(Math.min(100,Math.max(1,limit)));if(error)throw error;return hydrate(data??[]);}
export async function toggleThreadQuoteLike(quoteId:string,userId:string){const {data,error}=await supabase.from('thread_quote_likes').select('id').eq('quote_id',quoteId).eq('user_id',userId).maybeSingle();if(error)throw error;const active=!data;const q=active?supabase.from('thread_quote_likes').insert({quote_id:quoteId,user_id:userId}):supabase.from('thread_quote_likes').delete().eq('quote_id',quoteId).eq('user_id',userId);const r=await q;if(r.error)throw r.error;return active;}
async function hydrate(rows:any[]){const ids=[...new Set(rows.map(r=>r.user_id))];if(!ids.length)return rows.map(r=>({...r,profile:null}));const {data,error}=await supabase.from('profiles').select('id,username,display_name,avatar_url,verified').in('id',ids);if(error)throw error;const m=new Map((data??[]).map((p:any)=>[p.id,p]));return rows.map(r=>({...r,profile:m.get(r.user_id)??null}));}