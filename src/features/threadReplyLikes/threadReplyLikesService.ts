import { supabase } from '@/lib/supabase';
export async function listThreadReplyLikes(replyId:string,limit=50){const {data,error}=await supabase.from('thread_reply_likes').select('id,user_id,reply_id,created_at').eq('reply_id',replyId).order('created_at',{ascending:false}).limit(Math.min(100,Math.max(1,limit)));if(error)throw error;return hydrate(data??[]);}
export async function toggleThreadReplyLike(replyId:string,userId:string){const {data,error}=await supabase.from('thread_reply_likes').select('id').eq('reply_id',replyId).eq('user_id',userId).maybeSingle();if(error)throw error;const active=!data;const q=active?supabase.from('thread_reply_likes').insert({reply_id:replyId,user_id:userId}):supabase.from('thread_reply_likes').delete().eq('reply_id',replyId).eq('user_id',userId);const r=await q;if(r.error)throw r.error;return active;}
async function hydrate(rows:any[]){const ids=[...new Set(rows.map(r=>r.user_id))];if(!ids.length)return rows.map(r=>({...r,profile:null}));const {data,error}=await supabase.from('profiles').select('id,username,display_name,avatar_url,verified').in('id',ids);if(error)throw error;const m=new Map((data??[]).map((p:any)=>[p.id,p]));return rows.map(r=>({...r,profile:m.get(r.user_id)??null}));}