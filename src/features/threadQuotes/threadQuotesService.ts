import { supabase } from '@/lib/supabase';
export async function listThreadQuotes(threadId:string,limit=50){const {data,error}=await supabase.from('thread_quotes').select('id,thread_id,user_id,content,media_urls,created_at').eq('thread_id',threadId).order('created_at',{ascending:false}).limit(Math.min(100,Math.max(1,limit)));if(error)throw error;return hydrate(data??[]);}
export async function createThreadQuote(threadId:string,userId:string,content:string,media_urls:any[]=[]){const {data,error}=await supabase.from('thread_quotes').insert({thread_id:threadId,user_id:userId,content:content.trim(),media_urls}).select('*').single();if(error)throw error;return data;}
async function hydrate(rows:any[]){const ids=[...new Set(rows.map(r=>r.user_id))];if(!ids.length)return rows.map(r=>({...r,profile:null}));const {data,error}=await supabase.from('profiles').select('id,username,display_name,avatar_url,verified').in('id',ids);if(error)throw error;const m=new Map((data??[]).map((p:any)=>[p.id,p]));return rows.map(r=>({...r,profile:m.get(r.user_id)??null}));}