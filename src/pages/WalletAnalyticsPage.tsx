import WalletPage from '@/pages/WalletPage';

/** Standalone wallet surface: Wallet analytics. */
export default function WalletAnalyticsPage() {
  return <WalletPage initialTab="analytics" standaloneTitle="Wallet analytics" />;
}
