import { useEffect, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { supabase } from '@/lib/supabase';
import { useAuth } from '@/hooks/useAuth';
import { TopBar } from '@/components/layout/TopBar';
import { BarChart3, Plus, Loader2, CheckCircle2, Clock3, Users, Home, X, ImagePlus } from 'lucide-react';
import { toast } from 'sonner';
import { uploadMedia, deleteMedia } from '@/lib/media';

type PollOption={id:string;label:string;position:number;votes:number};
type Poll={id:string;question:string;description?:string;status:string;ends_at?:string|null;total_votes:number;options:PollOption[];has_voted?:boolean;media_url?:string|null;media_type?:'image'|'video'|null};

function PollCard({poll,onVoted}:{poll:Poll;onVoted:()=>void}){
 const {user}=useAuth(); const [selected,setSelected]=useState<string[]>([]); const [busy,setBusy]=useState(false);
 const vote=async()=>{if(!user){toast.error('Sign in to vote');return} if(!selected.length)return;
  setBusy(true); const {error}=await supabase.rpc('cast_poll_vote',{p_poll_id:poll.id,p_option_ids:selected});
  if(error)toast.error(error.message); else {toast.success('Vote recorded');onVoted()} setBusy(false);
 };
 const total=Math.max(1,poll.total_votes);
 return <article className="rounded-2xl border border-border bg-card p-4 space-y-4">
  <div className="flex items-start gap-3"><div className="w-9 h-9 rounded-xl bg-primary/10 text-primary flex items-center justify-center"><BarChart3 className="w-4 h-4"/></div><div className="flex-1"><h2 className="font-bold leading-snug">{poll.question}</h2>{poll.description&&<p className="text-xs text-muted-foreground mt-1">{poll.description}</p>}</div></div>
  {poll.media_url&&<div className="overflow-hidden rounded-2xl border border-border bg-muted">{poll.media_type==='video'?<video src={poll.media_url} controls playsInline preload="metadata" className="w-full max-h-80 object-cover"/>:<img src={poll.media_url} alt="Poll media" loading="lazy" className="w-full max-h-80 object-cover"/>}</div>}<div className="space-y-2">{poll.options.map(o=>{const pct=Math.round((o.votes/total)*100);const chosen=selected.includes(o.id);return <button key={o.id} disabled={busy||poll.status!=='open'} onClick={()=>setSelected(chosen?[]:[o.id])} className="w-full text-left rounded-xl border border-border p-3 relative overflow-hidden hover:border-primary/50 transition-colors"><div className="absolute inset-y-0 left-0 bg-primary/10" style={{width:`${pct}%`}}/><span className="relative flex items-center justify-between gap-3"><span className={chosen?'font-bold text-primary':'font-medium'}>{chosen?'✓ ':''}{o.label}</span><span className="text-xs text-muted-foreground">{pct}% · {o.votes}</span></span></button>})}</div>
  <div className="flex items-center justify-between text-xs text-muted-foreground"><span className="flex items-center gap-1"><Users className="w-3.5 h-3.5"/>{poll.total_votes} vote{poll.total_votes===1?'':'s'}</span>{poll.ends_at?<span className="flex items-center gap-1"><Clock3 className="w-3.5 h-3.5"/>{new Date(poll.ends_at).toLocaleString()}</span>:<span>Open voting</span>}</div>
  {poll.status==='open'&&<button disabled={busy||!selected.length} onClick={vote} className="w-full py-2.5 rounded-xl bg-primary text-primary-foreground font-bold disabled:opacity-40 flex items-center justify-center gap-2">{busy?<Loader2 className="w-4 h-4 animate-spin"/>:<CheckCircle2 className="w-4 h-4"/>}Cast vote</button>}
 </article>
}

export default function PollsPage(){
 const {user}=useAuth(); const navigate=useNavigate(); const [polls,setPolls]=useState<Poll[]>([]); const [loading,setLoading]=useState(true); const [creating,setCreating]=useState(false); const [question,setQuestion]=useState(''); const [description,setDescription]=useState(''); const [answers,setAnswers]=useState(['','']); const [endsAt,setEndsAt]=useState(''); const [media,setMedia]=useState<File|null>(null); const [mediaPreview,setMediaPreview]=useState<string|null>(null); const [mediaBusy,setMediaBusy]=useState(false);
 const load=async()=>{setLoading(true);const {data,error}=await supabase.from('polls').select('id,question,description,status,ends_at,posts:post_id(image_url,video_url,media_urls,is_video)').order('created_at',{ascending:false}).limit(20);if(error){toast.error(error.message);setLoading(false);return}const rows=await Promise.all((data??[]).map(async p=>{const {data:r}=await supabase.rpc('poll_results',{p_poll_id:p.id});const x=r as any;const post=(p as any).posts;const mediaUrl=post?.media_urls?.[0]??post?.image_url??post?.video_url??null;const mediaType=post?.is_video?'video':mediaUrl?'image':null;return {...p,total_votes:Number(x?.total_votes??0),options:(x?.options??[]) as PollOption[],media_url:mediaUrl,media_type:mediaType}}));setPolls(rows);setLoading(false)};
 useEffect(()=>{load()},[]); // eslint-disable-line react-hooks/exhaustive-deps
 const create=async()=>{const opts=answers.map(x=>x.trim()).filter(Boolean);if(question.trim().length<5||opts.length<2){toast.error('Add a clear question and at least 2 answers');return}setCreating(true);let uploadedId:string|null=null;try{if(media){setMediaBusy(true);const uploaded=await uploadMedia(media);uploadedId=uploaded.media_id;}const {error}=await supabase.rpc('create_poll',{p_question:question.trim(),p_options:opts,p_description:description.trim()||null,p_ends_at:endsAt?new Date(endsAt).toISOString():null,p_allow_multiple:false,p_visibility:'public',p_media_asset_id:uploadedId});if(error)throw error;toast.success('Poll published');setQuestion('');setDescription('');setAnswers(['','']);setEndsAt('');setMedia(null);if(mediaPreview)URL.revokeObjectURL(mediaPreview);setMediaPreview(null);await load()}catch(error:any){if(uploadedId)await deleteMedia(uploadedId).catch(()=>{});toast.error(error?.message??'Unable to publish poll')}finally{setMediaBusy(false);setCreating(false)}};
 return <div className="min-h-screen bg-background pb-16 lg:pb-0"><TopBar title="Polls"/><div className="p-4 space-y-5">
  <div className="flex items-center gap-2"><button onClick={()=>navigate('/')} className="p-2 rounded-xl hover:bg-muted"><Home className="w-4 h-4"/></button><div><h1 className="text-xl font-black">Community Polls</h1><p className="text-xs text-muted-foreground">Ask a question, collect answers, and publish transparent vote totals.</p></div></div>
  {user&&<section className="rounded-2xl border border-primary/20 bg-primary/5 p-4 space-y-3"><div className="flex items-center gap-2 font-bold"><Plus className="w-4 h-4"/>Create a poll</div><input value={question} onChange={e=>setQuestion(e.target.value)} maxLength={500} placeholder="What should the community decide?" className="w-full rounded-xl border border-border bg-background px-3 py-2.5 text-sm"/><textarea value={description} onChange={e=>setDescription(e.target.value)} maxLength={1000} placeholder="Context (optional)" rows={2} className="w-full rounded-xl border border-border bg-background px-3 py-2.5 text-sm resize-none"/><div className="space-y-2">{answers.map((a,i)=><div key={i} className="flex gap-2"><input value={a} onChange={e=>setAnswers(xs=>xs.map((x,j)=>j===i?e.target.value:x))} maxLength={200} placeholder={`Answer ${i+1}`} className="flex-1 rounded-xl border border-border bg-background px-3 py-2.5 text-sm"/>{answers.length>2&&<button onClick={()=>setAnswers(xs=>xs.filter((_,j)=>j!==i))} className="p-2 text-muted-foreground"><X className="w-4 h-4"/></button>}</div>)}</div><button disabled={answers.length>=8} onClick={()=>setAnswers(x=>[...x,''])} className="text-xs font-bold text-primary disabled:opacity-40">+ Add answer</button><div className="space-y-2"><label className="text-xs font-semibold text-muted-foreground">Poll media (optional)</label><input id="poll-media" type="file" accept="image/jpeg,image/png,image/gif,image/webp,image/avif,video/mp4,video/webm,video/quicktime" className="hidden" onChange={e=>{const file=e.target.files?.[0]??null;if(!file)return;if(mediaPreview)URL.revokeObjectURL(mediaPreview);setMedia(file);setMediaPreview(URL.createObjectURL(file))}}/><label htmlFor="poll-media" className="inline-flex cursor-pointer items-center gap-2 rounded-xl border border-dashed border-primary/40 bg-primary/5 px-3 py-2 text-xs font-bold text-primary hover:bg-primary/10"><ImagePlus className="w-4 h-4"/>Add photo or video</label>{mediaPreview&&<div className="relative overflow-hidden rounded-2xl border border-border bg-muted">{media?.type.startsWith('video/')?<video src={mediaPreview} muted playsInline controls className="w-full max-h-56 object-cover"/>:<img src={mediaPreview} alt="Poll preview" className="w-full max-h-56 object-cover"/>}<button type="button" onClick={()=>{if(mediaPreview)URL.revokeObjectURL(mediaPreview);setMedia(null);setMediaPreview(null)}} className="absolute right-2 top-2 rounded-full bg-black/70 p-1.5 text-white"><X className="w-4 h-4"/></button></div>}</div><div><label className="text-xs font-semibold text-muted-foreground">Voting closes (optional)</label><input type="datetime-local" value={endsAt} onChange={e=>setEndsAt(e.target.value)} className="mt-1 w-full rounded-xl border border-border bg-background px-3 py-2 text-sm"/></div><button onClick={create} disabled={creating||mediaBusy} className="w-full rounded-xl bg-primary text-primary-foreground py-2.5 font-bold flex justify-center gap-2">{creating?<Loader2 className="w-4 h-4 animate-spin"/>:<Plus className="w-4 h-4"/>}Publish poll</button></section>}
  {loading?<div className="py-16 flex justify-center"><Loader2 className="w-6 h-6 animate-spin"/></div>:polls.length?<div className="space-y-3">{polls.map(p=><PollCard key={p.id} poll={p} onVoted={load}/>)}</div>:<div className="text-center py-16 text-muted-foreground">No public polls yet. Be the first to ask a question.</div>}
 </div></div>
}
