import WalletPage from '@/pages/WalletPage';

/** Standalone wallet surface: Transaction reminders. */
export default function WalletRemindersPage() {
  return <WalletPage initialTab="reminders" standaloneTitle="Transaction reminders" />;
}
