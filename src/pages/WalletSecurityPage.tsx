import WalletPage from '@/pages/WalletPage';

/** Standalone wallet surface: Wallet security. */
export default function WalletSecurityPage() {
  return <WalletPage initialTab="security" standaloneTitle="Wallet security" />;
}
