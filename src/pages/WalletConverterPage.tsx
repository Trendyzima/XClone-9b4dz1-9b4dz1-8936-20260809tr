import WalletPage from '@/pages/WalletPage';

/** Standalone wallet surface: Currency converter. */
export default function WalletConverterPage() {
  return <WalletPage initialTab="converter" standaloneTitle="Currency converter" />;
}
