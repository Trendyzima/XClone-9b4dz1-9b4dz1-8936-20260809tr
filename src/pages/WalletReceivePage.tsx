import WalletPage from '@/pages/WalletPage';

/** Standalone wallet surface: Receive money. */
export default function WalletReceivePage() {
  return <WalletPage initialTab="receive" standaloneTitle="Receive money" />;
}
