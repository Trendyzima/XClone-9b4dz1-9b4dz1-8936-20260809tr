import WalletPage from '@/pages/WalletPage';

/** Standalone wallet surface: Wallet history. */
export default function WalletHistoryPage() {
  return <WalletPage initialTab="history" standaloneTitle="Wallet history" />;
}
