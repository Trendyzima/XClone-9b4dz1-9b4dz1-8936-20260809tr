import WalletPage from '@/pages/WalletPage';

/** Standalone wallet surface: Send money. */
export default function WalletSendPage() {
  return <WalletPage initialTab="send" standaloneTitle="Send money" />;
}
