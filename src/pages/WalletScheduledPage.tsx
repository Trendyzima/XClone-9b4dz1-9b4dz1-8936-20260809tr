import WalletPage from '@/pages/WalletPage';

/** Standalone wallet surface: Scheduled transfers. */
export default function WalletScheduledPage() {
  return <WalletPage initialTab="scheduled" standaloneTitle="Scheduled transfers" />;
}
